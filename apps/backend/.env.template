STORE_CORS=http://localhost:8000,https://konduit.co.zw,https://www.konduit.co.zw
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://admin.konduit.co.zw
AUTH_CORS=http://localhost:5173,http://localhost:9000,http://localhost:8000,https://konduit.co.zw,https://admin.konduit.co.zw
# Upstash Redis (TLS). Claim console: see deploy/INFRA.md
# Format: rediss://default:PASSWORD@HOST.upstash.io:6379
REDIS_URL=
JWT_SECRET=change-me-jwt-secret
COOKIE_SECRET=change-me-cookie-secret
DATABASE_URL=postgres://USER:PASSWORD@HOST/DB?sslmode=require
DB_NAME=konduit

# Paynow (sandbox first — never commit live keys)
PAYNOW_INTEGRATION_ID=
PAYNOW_INTEGRATION_KEY=
PAYNOW_RESULT_URL=http://localhost:9000/hooks/payment/paynow_paynow
PAYNOW_RETURN_URL=http://localhost:8000/api/paynow/return
PAYNOW_DEBUG=true

# Meilisearch — use Admin key on backend; Search key on storefront only
# Production: Meilisearch Cloud or Railway Docker (see deploy/INFRA.md)
MEILISEARCH_HOST=
MEILISEARCH_API_KEY=

# WhatsApp Business Cloud API (order status updates)
WHATSAPP_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_API_VERSION=v21.0
